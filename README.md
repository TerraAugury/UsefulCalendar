# UsefulCalendar

Appointment Notebook WebApp built with HTML, CSS, and vanilla JS.

## Run locally

- Open `index.html` in a browser.

## GitHub Pages

- Set Pages to deploy from the root of the `main` branch.
- Entry point: `index.html` (loads assets from `app/`).
